#include "Property.h"

class Residence : public Property{
 int numOwn;
	public:
 Residence(std::string name,int site, BoardDisplay* board);
 int getFee();
 int getValue();
 void setNumOwn(int n);
}
